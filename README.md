# CourtRAG
CourtRAG A REST API that lets you upload Ukrainian legal documents (PDFs), stores them as vector embeddings in Qdrant, and answers natural language questions about them using GPT-4 — returning the answer along with the exact source documents it was based on.
